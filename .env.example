# Copy to .env and fill in. The bot reads these env vars when the matching
# *_env field is set in config.json (e.g. wallet.private_key_env).
# Loaded by systemd via EnvironmentFile= or by docker-compose env_file:.

POLYMARKET_PRIVATE_KEY=
POLYMARKET_FUNDER_ADDRESS=
