services:
  weatherbet:
    build: .
    container_name: weatherbet
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.json:/app/config.json
      - ./data:/app/data
    command: ["python", "bot_v2.py", "run"]
